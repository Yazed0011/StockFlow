# StockFlow
StockFlow API is a simple RESTful backend for managing small-business inventory, invoices, products, categories, suppliers, and stock movements (in/out). It is built with pure PHP and MySQL using PDO, JWT-based authentication, and JSON responses
